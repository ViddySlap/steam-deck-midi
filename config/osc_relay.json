{
  "_comment": "Resolume Arena's OSC output supports exactly ONE target (Preferences/osc.xml has a single <Output> element). This relay fans it out. Point Resolume's OSC Output at the 'listen' address below, and it reaches every destination. Destinations keep port 7002, so the TouchOSC patches need no changes.",
  "enabled": true,
  "listen": "127.0.0.1:7010",
  "destinations": [
    "10.10.10.5:7002",
    "10.10.10.30:7001"
  ]
}
